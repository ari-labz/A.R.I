namespace ARI.Calendar;

/// <summary>Config for the calendar module (lives under Modules.Calendar in AriConfig).</summary>
public class CalendarConfig
{
    public bool Enabled { get; init; } = true;
}
