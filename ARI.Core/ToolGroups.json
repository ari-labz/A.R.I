{
  "Groups": {
    "git_tools": {
      "Description": "Git status, diff, log, and commit for the current vault/repo.",
      "Tools": ["git_status", "git_diff", "git_log", "git_commit"]
    },
    "project_git_tools": {
      "Description": "Git operations (status, fetch, pull, log, diff, add, commit, push, branch, checkout, stash, restore) across the project's repositories, plus cloning a new GitHub repo into the project with git_clone. Repos are auto-discovered — use this when working with a project that contains multiple git repos.",
      "Tools": ["git", "git_clone"]
    },
    "github_tools": {
      "Description": "Full GitHub access over the REST API: view a repo, list/view issues and pull requests, list Actions runs, list repos you or an org have, create a new repo (public or private), or fork one. Reading public repos always works; private repos, creating, and forking need a GitHub account connected in the control panel (Integrations → GitHub) first — this can't be set up from chat. Pair with git_clone (project_git_tools) to bring a repo onto disk after finding/creating/forking it.",
      "Tools": ["github"]
    },
    "filesystem_tools": {
      "Description": "Work with files: preview, read, list, search, find, edit, write, and deliver a file to the user as a download. Files live in a filesystem — either a bound project (persistent) or a scratchpad (ephemeral, deleted with the conversation). If you have neither and need to write or juggle files (e.g. to give the user a file), call create_scratchpad first to get an ephemeral filesystem; you do NOT need a project.",
      "Tools": ["create_scratchpad", "preview_file", "read_file", "list_directory", "search_files", "find_files", "edit_file", "write_file", "deliver_file"]
    },
    "coding_tools": {
      "Description": "Build the current project to verify edits compile.",
      "Tools": ["build_project"]
    },
    "memory_tools": {
      "Description": "Read and write Ari's personal brain (memory graph) — never through the generic filesystem tools. search_brain finds notes by title/alias/content — returns 'title — name' pairs. recall_memory reads the full content of a note by that name. create_memory adds a new note (fails if the name already exists); edit_memory replaces an existing note's content (fails if the name does NOT exist — and optionally renames/retypes it); delete_memory removes one. All three take a commit_message explaining what changed and why, and commit to the brain git repo immediately, one commit per note. neighbours shows graph connections around a note. These tools access Ari's own stored memories, not a project filesystem.",
      "Tools": ["search_brain", "recall_memory", "create_memory", "edit_memory", "delete_memory", "neighbours"]
    },
    "vault_tools": {
      "Description": "Search an Obsidian note graph within the bound project (regex, no index). Only useful when the project is an Obsidian vault — calling this on a code repository will return nothing. For searching your own memory, use search_brain / recall_memory instead.",
      "Tools": ["search_vault"]
    },
    "project_tools": {
      "Description": "List, create, rename, bind, and set the filesystem path of projects — the workspaces (code repos or note vaults) a conversation can be scoped to. Use this for any request to start/open/make a new project, workspace, or \"project file\" — that phrase means a new project, not a file inside one. If the user names a specific directory on their machine, confirm the exact path with them, then pass it to create_project (new project) or set_project_path (existing project) — never RemoteFs, which needs a connected desktop app and can't be pointed anywhere from chat.",
      "Tools": ["list_projects", "create_project", "rename_project", "bind_project", "set_project_path"]
    },
    "calendar_tools": {
      "Description": "ARI's own calendar. create_event adds context (working hours, a birthday, a trip) that is never acted on by itself. create_reminder schedules a real turn to fire later — write its prompt as an instruction to your future self, not the final wording. Both support repeat (daily/weekly/monthly/yearly, optionally an end date or count). list_events looks forward or backward from today by a signed day count. delete_entry removes either kind by id.",
      "Tools": ["create_event", "create_reminder", "list_events", "delete_entry"]
    },
    "persona_tools": {
      "Description": "Propose a change to your own persona. Load this when the user criticises HOW you replied — your tone, a verbal tic, a habit they find annoying — since promising to change does nothing on its own; your persona is what actually drives your behaviour.",
      "Tools": ["propose_persona_edit"]
    },
    "image_tools": {
      "Description": "Generate images using Stable Diffusion via ComfyUI. Supports text-to-image and img2img (pass reference_images from the user's attachments to guide composition). Prefer higher denoise (0.9–1.0) for creative freedom; lower (0.5–0.75) to stay close to a reference. Generating only shows you the result — call present_image afterward to reveal it to the user, or retry with a revised prompt if it's wrong.",
      "Tools": ["generate_image", "present_image"]
    },
    "discord_tools": {
      "Description": "Discord voice-channel control: find which VC a user is in, join a voice channel, or leave one.",
      "Tools": ["discord_list_voice_channels", "discord_join_voice_channel", "discord_leave_voice_channel"]
    },
    "web_tools": {
      "Description": "Search the web and read web pages. Use for current events, factual lookups, or anything time-sensitive.",
      "Tools": ["search_web", "fetch_page"]
    }
  }
}
